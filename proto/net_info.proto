// 描述网络接口的相关信息，包括名称、发送和接收速率、发送和接收包速率

syntax = "proto3";
package monitor.proto;

message NetInfo {
    string name = 1;
    float send_rate = 2;
    float rcv_rate = 3;
    float send_packets_rate = 4;
    float rcv_packets_rate = 5;
  }